<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="end"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <TextView
        android:id="@+id/userId"
        android:layout_marginTop="10dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_width="200dp"
        android:singleLine="true"
        android:text="dfhkwhfuwhfu223dfafsdfwfd"
        android:ellipsize="end"
        android:textColor="@color/black"
        android:textStyle="bold"
        android:paddingStart="10dp"
        android:layout_marginEnd="10dp"
        android:textSize="20dp"
        android:layout_height="wrap_content"/>

    <TextView
        android:id="@+id/userChatText"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:textColor="@color/black"
        android:layout_marginTop="10dp"
        android:text="안녕하세요."
        android:textSize="15dp"
        android:paddingStart="10dp"
        app:layout_constraintTop_toBottomOf="@id/userId"
        app:layout_constraintStart_toStartOf="@id/userId"

        />
    <TextView
        android:id="@+id/sendTime"
        app:layout_constraintTop_toTopOf="@id/userChatText"
        app:layout_constraintStart_toEndOf="@id/userChatText"
        app:layout_constraintBottom_toBottomOf="@id/userChatText"
        android:text="10:22"
        app:layout_constraintHorizontal_chainStyle="packed"
        android:layout_marginStart="10dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>
</androidx.constraintlayout.widget.ConstraintLayout>